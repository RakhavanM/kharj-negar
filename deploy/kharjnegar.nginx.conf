# Install as /etc/nginx/sites-available/kharjnegar.raminakhavan.ir
# Keep the existing Hermes virtual host unchanged.
server {
    listen 80;
    listen [::]:80;
    server_name kharjnegar.raminakhavan.ir;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name kharjnegar.raminakhavan.ir;

    ssl_certificate /etc/letsencrypt/live/kharjnegar.raminakhavan.ir/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/kharjnegar.raminakhavan.ir/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    root /opt/kharj-negar/frontend;
    index index.html;

    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
